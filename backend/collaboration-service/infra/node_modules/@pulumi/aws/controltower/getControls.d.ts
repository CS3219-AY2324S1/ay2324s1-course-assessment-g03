import * as pulumi from "@pulumi/pulumi";
/**
 * List of Control Tower controls applied to an OU.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const thisOrganization = aws.organizations.getOrganization({});
 * const thisOrganizationalUnits = thisOrganization.then(thisOrganization => aws.organizations.getOrganizationalUnits({
 *     parentId: thisOrganization.roots?.[0]?.id,
 * }));
 * const thisControls = thisOrganizationalUnits.then(thisOrganizationalUnits => aws.controltower.getControls({
 *     targetIdentifier: .filter(x => x.name == "Security").map(x => (x.arn))[0],
 * }));
 * ```
 */
export declare function getControls(args: GetControlsArgs, opts?: pulumi.InvokeOptions): Promise<GetControlsResult>;
/**
 * A collection of arguments for invoking getControls.
 */
export interface GetControlsArgs {
    /**
     * The ARN of the organizational unit.
     */
    targetIdentifier: string;
}
/**
 * A collection of values returned by getControls.
 */
export interface GetControlsResult {
    /**
     * List of all the ARNs for the controls applied to the `targetIdentifier`.
     */
    readonly enabledControls: string[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly targetIdentifier: string;
}
/**
 * List of Control Tower controls applied to an OU.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const thisOrganization = aws.organizations.getOrganization({});
 * const thisOrganizationalUnits = thisOrganization.then(thisOrganization => aws.organizations.getOrganizationalUnits({
 *     parentId: thisOrganization.roots?.[0]?.id,
 * }));
 * const thisControls = thisOrganizationalUnits.then(thisOrganizationalUnits => aws.controltower.getControls({
 *     targetIdentifier: .filter(x => x.name == "Security").map(x => (x.arn))[0],
 * }));
 * ```
 */
export declare function getControlsOutput(args: GetControlsOutputArgs, opts?: pulumi.InvokeOptions): pulumi.Output<GetControlsResult>;
/**
 * A collection of arguments for invoking getControls.
 */
export interface GetControlsOutputArgs {
    /**
     * The ARN of the organizational unit.
     */
    targetIdentifier: pulumi.Input<string>;
}
